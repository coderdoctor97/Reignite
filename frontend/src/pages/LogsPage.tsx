/** Logs — structured event log viewer. Phase 1.1: placeholder. */

export function LogsPage() {
  return (
    <div className="page">
      <h1 className="page-title">Logs</h1>
      <p className="page-description">
        View structured events: gateway lifecycle, rotation, errors, and health checks.
      </p>
      <div className="page-placeholder">
        <p>Log viewer will appear here.</p>
      </div>
    </div>
  );
}
