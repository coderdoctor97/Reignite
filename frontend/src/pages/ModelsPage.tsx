/** Models — model configuration and routing. Phase 1.1: placeholder. */

export function ModelsPage() {
  return (
    <div className="page">
      <h1 className="page-title">Models</h1>
      <p className="page-description">
        Configure available models, set defaults, and manage fallback chains.
      </p>
      <div className="page-placeholder">
        <p>Model management will appear here.</p>
      </div>
    </div>
  );
}
