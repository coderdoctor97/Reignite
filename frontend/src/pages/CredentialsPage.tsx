/** Credentials — manual entry and auto-discovery. Phase 1.1: placeholder. */

export function CredentialsPage() {
  return (
    <div className="page">
      <h1 className="page-title">Credentials</h1>
      <p className="page-description">
        Manage API keys. Enter manually or discover automatically from the provider.
      </p>
      <div className="page-placeholder">
        <p>Credential management will appear here.</p>
      </div>
    </div>
  );
}
