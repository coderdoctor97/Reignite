/** Sessions — session credential management. Phase 1.1: placeholder. */

export function SessionsPage() {
  return (
    <div className="page">
      <h1 className="page-title">Sessions</h1>
      <p className="page-description">
        Manage session credentials for provider dashboard access. Validate and replace sessions.
      </p>
      <div className="page-placeholder">
        <p>Session management will appear here.</p>
      </div>
    </div>
  );
}
