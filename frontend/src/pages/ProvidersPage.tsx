/** Providers — upstream provider management. Phase 1.1: placeholder. */

export function ProvidersPage() {
  return (
    <div className="page">
      <h1 className="page-title">Providers</h1>
      <p className="page-description">
        Configure upstream API providers. Set endpoints, authentication, and health monitoring.
      </p>
      <div className="page-placeholder">
        <p>Provider management will appear here.</p>
      </div>
    </div>
  );
}
